name: Benchmark

on:
  push:
    branches: [ main ]
    paths:
      - '**'
      - '!docs/**' # ignore docs changes
      - '!**.md' # ignore markdown changes
  pull_request:
    branches: [ main ]
    paths:
      - '**.go'
      - 'go.*'
      - 'cmd/go.*'
      - 'Makefile'
      - 'Dockerfile'
      - 'integration/**'
      - 'scripts/**'

permissions:
  contents: write
  deployments: write
      
jobs:
  benchmark:
    name: Performance regression check
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-go@v4
        with:
          go-version: '1.18.10'
      - run: make
<<<<<<< HEAD
      - name: Run benchmark
        run: make benchmarks-perf-test
      - run: cd benchmark/performanceTest && cat output/results.json
      
=======
      # - name: Run benchmark
      #   run: make benchmarks-test 
      # - name: Check benchmark result
      #   run: cat ${{ github.workspace }}/benchmark/performanceTest/output/results.json | tee benchmark.json
      # Download previous benchmark result from cache (if exists)
      - name: Download previous benchmark data
        uses: actions/cache@v1
        with:
          path: ./cache
          key: performance-benchmark
      # - name: Print Working Directory
      #   run: pwd
      # - name: Copy benchmark data to cache
      #   run: cp ${{ github.workspace }}/benchmark.json ./cache
      # - name: Upload benchmark data to cache
      #   uses: actions/upload-artifact@v2
      #   with:
      #     name: performance-benchmark
      #     path: ./cache
      - name: Store benchmark result
        uses: benchmark-action/github-action-benchmark@v1
        with:
          name: Soci Benchmark
          tool: 'go'
          benchmark-data-dir-path: "dev/bench/perfTest"
          output-file-path: ${{github.workspace}}/test.txt
          github-token: ${{ secrets.GITHUB_TOKEN }}
          auto-push: true
          # Show alert with commit comment on detecting possible performance regression
          alert-threshold: '200%'
          comment-on-alert: true
          fail-on-alert: true
      - name: Push benchmark result
        run: git push 'https://github.com/coderbirju/soci-snapshotter.git' gh-pages:gh-pages
>>>>>>> 5491bac (Add basic workflow)
